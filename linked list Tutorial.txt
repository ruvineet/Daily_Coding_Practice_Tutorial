//How to create a linked list

#include<bits/stdc++.h>
using namespace std;
struct Node
{
    int data;
    struct Node* next;
};
struct Node* head;
void Insert(int x);
void Print(int x);
void Insert(int x)
{
    Node* temp=(Node*)malloc(sizeof(struct Node));
    temp->data=x;
    temp->next=head;
    head= temp;
}
void Print(int x)
{
    struct Node* temp = head;
    while(temp!=NULL)
    {
        cout<<temp->data<<" ";
        temp=temp->next;
    }
   // cout<<endl;
    
}
int main()
{
    head=NULL;
    cout<<"how many numbers "<<endl;
    int n,x;
    cin>>n;
    for(int i=0;i<n;i++)
    {
      cout<<"Enter the number "<<endl;
      cin>>x;
      Insert(x);
      Print(x);
    }
} 

Correct Programme

//UPDATE LINKED LIST CODE ISSUE IN DELETE IN FRONT NODE FUNCTION

//How to create a linked list

#include<bits/stdc++.h>
using namespace std;
struct Node
{
    int data;
    struct Node* next;
};
struct Node* head;
void Insert(int x);
void Print();
void delete_end_node();
Node* add_element_end(int x);
Node* delete_front();
Node* delete_front()
{
    Node* temp;
    if(head==NULL ) 
    {
        cout<<"not possible sir"<<endl;
    }
    else if(head->next==nullptr)
    {
        head=NULL;
        free(head);
        cout<<"all node deleted"<<endl;
    }
    else
    {
        temp=head;
        head=head->next;
        free(temp);
    }
    return head;
}
void Insert(int x)
{
    Node* temp=(Node*)malloc(sizeof(struct Node));
    temp->data=x;
    temp->next=head;
    head= temp;
}
void Print()
{
    struct Node* temp = head;
    while(temp!=NULL)
    {
        cout<<temp->data<<" ";
        temp=temp->next;
    }
    
}
void delete_end_node()
{
    struct Node *ptr, *p1;
    if(head==NULL)
    {
        cout<<"can not delete_end_node"<<endl;
    }
    else
    {
        ptr=head;
        while(ptr->next!=NULL)
        {
            p1=ptr;
            ptr=ptr->next;
        }
        p1->next=ptr->next;
        free(ptr);
    }
}
struct Node* add_element_end(int x)
{
     Node* temp=(Node*)malloc(sizeof(struct Node));
     cout<<"enter the data"<<endl;
     cin>>x;
     temp->data=x;
     Node* temp2=head;
     while(temp2->next!=NULL)
     {
         temp2=temp2->next;
     }
     temp2->next=temp;
     temp->next=NULL;
     temp2=temp;
     return head;
}
int main()
{
    head=NULL;
    cout<<"how many numbers "<<endl;
    int n,x;
    cin>>n;
    for(int i=0;i<n;i++)
    {
      cout<<"Enter the number "<<endl;
      cin>>x;
      Insert(x);
      Print();
    }
    string s1;
    cout<<"add the element in end"<<endl;
    add_element_end(x);
    Print();
    cout<<"you want to delete end node - Y/N"<<endl;
    cin>>s1;
    if(s1=="Y")
    {
        delete_end_node();
        cout<<"updated list"<<endl;
        Print();
    }
    else{
        cout<<" thanks"<<endl;
    }
    cout<<"you want to delete from front"<<endl;
    head=delete_front();
    cout<<"updated list after delete_front element" <<endl;
    Print();
    return 0;
    
} 

//full linked list withi Reverse the linked list

//UPDATE LINKED LIST CODE ISSUE IN DELETE IN FRONT NODE FUNCTION

//How to create a linked list

#include<bits/stdc++.h>
using namespace std;
struct Node
{
    int data;
    struct Node* next;
};
struct Node* head;
void make_cycle(Node* &head, int pos);
void Insert(int x);
void Print();
void delete_end_node();
Node* add_element_end(int x);
Node* delete_front();
Node* reverse();
void detect_loop(Node* &head);
void make_cycle(Node*  &head, int pos)
{
    int count=1;
    Node  *startnode, *temp=head;
    while(temp->next!=NULL)
    {
        if(pos==count)
        {
            startnode = temp;
        }
        temp=temp->next;
        count++;
    }
    startnode=temp->next;
  
    
}
void detect_loop(Node* &head)
{
    
    Node *slow , *fast;
    slow=head;
    fast=head;
    while(fast!=nullptr && fast->next!=NULL)
    {
        slow=slow->next;
        fast=fast->next->next;
        if(slow==fast)
        {
            cout<<" Yes loop is present "<<endl;
            break;
        }
    }
    cout<<" No loop"<<endl;
}
Node* reverse()
{
    struct Node *t1,*t2;
    t1=NULL;
    t2=NULL;
    while(head!=NULL)
    {
        t2=head->next;
        head->next=t1;
        t1=head;
        head=t2;
    }
    return t1;
}
Node* delete_front()
{
    Node* temp;
    if(head==NULL ) 
    {
        cout<<"not possible sir"<<endl;
    }
    else if(head->next==nullptr)
    {
        head=NULL;
        free(head);
        cout<<"all node deleted"<<endl;
    }
    else
    {
        temp=head;
        head=head->next;
        free(temp);
    }
    return head;
}
void Insert(int x)
{
    Node* temp=(Node*)malloc(sizeof(struct Node));
    temp->data=x;
    temp->next=head;
    head= temp;
}
void Print()
{
    struct Node* temp = head;
    while(temp!=NULL)
    {
        cout<<temp->data<<" ";
        temp=temp->next;
    }
    
}
void delete_end_node()
{
    struct Node *ptr, *p1;
    if(head==NULL)
    {
        cout<<"can not delete_end_node"<<endl;
    }
    else
    {
        ptr=head;
        while(ptr->next!=NULL)
        {
            p1=ptr;
            ptr=ptr->next;
        }
        p1->next=ptr->next;
        free(ptr);
    }
}
struct Node* add_element_end(int x)
{
     Node* temp=(Node*)malloc(sizeof(struct Node));
     cout<<"enter the data"<<endl;
     cin>>x;
     temp->data=x;
     Node* temp2=head;
     while(temp2->next!=NULL)
     {
         temp2=temp2->next;
     }
     temp2->next=temp;
     temp->next=NULL;
     temp2=temp;
     return head;
}
int main()
{
    head=NULL;
    cout<<"how many numbers "<<endl;
    int n,x;
    cin>>n;
    for(int i=0;i<n;i++)
    {
      cout<<"Enter the number "<<endl;
      cin>>x;
      Insert(x);
      Print();
    }
    string s1;
    cout<<"add the element in end"<<endl;
   // add_element_end(x);
    make_cycle(head, 3);
   // detect_loop(head);
    Print();
    cout<<"you want to delete end node - Y/N"<<endl;
    cin>>s1;
    if(s1=="Y")
    {
        delete_end_node();
        cout<<"updated list"<<endl;
        Print();
    }
    else{
        cout<<" thanks"<<endl;
    }
    cout<<"you want to delete from front"<<endl;
    head=delete_front();
    cout<<"updated list after delete_front element" <<endl;
    head=reverse();
    Print();
    return 0;
    
} 


//check linked list palindrome or not

 int checkPalindrome(vector<int>v2)
      {
          int s=0;
          int e=v2.size()-1;
          while(s<=e)
          {
              if(v2[s]!=v2[e])
              {
                  return 0;
              }
              s++;
              e--;
          }
          return 1;
      }
    //Function to check whether the list is palindrome.
    bool isPalindrome(Node *head)
    {
          vector<int>v1;
          Node* temp=head;
          while(temp!=NULL)
          {
             v1.push_back(temp->data);
             temp=temp->next;
          }
          
          return checkPalindrome(v1);
    } 

//optimized code linked list

class Solution{
  public:
  
  Node* get_mid(Node* head)
  {
      Node* slow =head;
      Node* fast=head->next;
      while(fast!=NULL && fast->next!=NULL)
      {
          slow=slow->next;
          fast=fast->next->next;
      }
      return slow;
  }
  
  Node* reverse(Node* head)
  {
      Node* start=head;
      Node* t1=NULL;
      Node* t2=NULL;
     while(start!=NULL)
     {
         t2=start->next;
         start->next=t1;
         t1=start;
         start=t2;
     }
     return t1;
      
  }
    //Function to check whether the list is palindrome.
    bool isPalindrome(Node *head)
    {
        if(head->next==NULL || head==nullptr)
        {
            return true;
        }
        
        Node* middle=get_mid(head);
        middle->next=reverse(middle->next);
        
        Node* head1=head;
        Node* temp2=middle->next;
        while(temp2!=NULL)
        {
            if(temp2->data!=head1->data)
            {
                return false;
            }
            temp2=temp2->next;
            head1=head1->next;
        }
        return true;
    }
};


//find the middle element in the linked list(Alternative way)


Node *findMiddle(Node *head) {
   int l=0;
   Node *temp=head;
   while(temp!=NULL)
   {
       temp=temp->next;
       l++;
   }
   int c=0;
   while(c<l/2)
   {
       head=head->next;
       c++;
   }
   return head;
}

//optimaized way

Node *findMiddle(Node *head) {
    Node *slow= head;
    Node *fast=head;
    while(slow && fast && fast->next!=NULL)
    {
          slow=slow->next;
          fast=fast->next->next;
    }
return slow;
}

//

// Delete nth node from end in LL

Node* removeKthNode(Node* head, int K)
{
    
   Node* p1=head;
   Node* p2=head;
   Node* prev=NULL;
   int c=1;
   Node* temp=NULL;
   while(c<=K-1)
   {
       temp=p1;
       p1=p1->next;
       c++;
   }
   if(p1->next!=NULL){
       while (p1->next != NULL) {
         prev = p2;
         p2 = p2->next;
         p1 = p1->next;
       }
       prev->next = p2->next;
       free(p2);
       return head;
   }
   else{
       temp=head->next;
       return temp;

   }
}

// how to swap nodes in pairs in linked list

Node* pairsSwap(Node *head)
{
    Node* curr=head;
    Node* prev=NULL;
    Node* far=NULL;
    int count=0;
    while(curr!=NULL && count<2)
    {
        far=curr->next;
        curr->next=prev;
        prev=curr;
        curr=far;
        count++;
    }
    if(far!=NULL)
    {
        head->next=pairsSwap(far);
    }
    return prev;

}

//remove duplicates from sored linked list
Node *removeDuplicates(Node *head)
{
  if(head==NULL)
  {
      return head;
  }
  Node* curr=head;
  while(curr!=NULL)
  {
      if( curr->next!=NULL && curr->data == curr->next->data)
      {
          Node* two_step=curr->next->next;
          Node* dlt_nd=curr->next;
          delete dlt_nd;
          curr->next=two_step;
         
      }
      else
      {
          curr=curr->next;
      }
  }
  return head;
}

//MERGE SORT IN LINKED LIST

node* findMid(node* head) {
    node* slow = head;
    node* fast = head -> next;
    
    while(fast != NULL && fast -> next != NULL) {
        slow = slow -> next;
        fast = fast -> next -> next; 
    }
    return slow;
}

node* merge(node* left, node* right) {
    
    if(left == NULL)
        return right;
    
    if(right == NULL)
        return left;
    
    node* ans = new node(-1);
    node* temp = ans;
    
    //merge 2 sorted Linked List
    while(left != NULL && right != NULL) {
        if(left -> data < right -> data ) {
            temp -> next = left;
            temp = left;
            left = left -> next;
        }
        else
        {
            temp -> next = right;
            temp = right;
            right = right -> next;
        }
    }
    
    while(left != NULL) {
        temp -> next = left;
        temp = left;
        left = left -> next;
    }
    
    while(right != NULL) {
        temp -> next = right;
        temp = right;
        right = right -> next;
    }
    
    ans = ans -> next;
    return ans;
    
}


node* mergeSort(node *head) {
    //base case
    if( head == NULL || head -> next == NULL ) {
        return head;
    }
    
    // break linked list into 2 halvs, after finding mid
    node* mid = findMid(head);
    
    node* left = head;
    node* right = mid->next;
    mid -> next = NULL;
    
    //recursive calls to sort both halves
    left = mergeSort(left);
    right = mergeSort(right);
    
    //merge both left and right halves
    node* result = merge(left, right);
    
    return result;
}